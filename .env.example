# API Configuration
VITE_API_BASE_URL=http://localhost:3000

# Google Maps (Places Autocomplete + Geocoding) - used by the branch
# location picker. Same key as the squarft-project-panel mobile app.
VITE_GOOGLE_MAPS_API_KEY=
